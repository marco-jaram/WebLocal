<section class="contenedor-contacto">
    <div class="contacto-grid contenedor">
        <div  class="contacto-texto">
            <h1></h1>
            <form
                target="_blank"
                action="https://formsubmit.co/marco,jaramillocast@gmail.com"
                method="POST"
            >
                <div class="contenido-form">
                    <div class="datos-form">
                        <input
                            type="text"
                            name="name"
                            class="form-control"
                            placeholder="Full Name"
                            required
                        />

                        <input
                            type="email"
                            name="email"
                            class="form-control"
                            placeholder="Email Address"
                            required
                        />
                    </div>

                    <div class="mensaje-form">
                        <textarea
                            placeholder="Your Message"
                            class="form-control"
                            name="message"
                            rows="10"
                            required></textarea>
                    </div>
                    <button type="submit" class="boton-contacto"
                        >Enviar</button
                    >
                </div>
            </form>
        </div>
    </div>

    <style>
        .datos-form {
            display: flex;
            flex-direction: column;
        }

        .contenido-form {
        }
    </style>
</section>
