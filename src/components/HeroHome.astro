<div class="hero">
    <div class="contenedor">
        <div class="titulares">
            <h1>
                Transforma tu conocimiento en un negocio digital automatizado<span class="blinking-cursor">|</span>
            </h1>
            <h2 class="subtitle">
                Dedícate a tus clientes, no a la tecnología. Construyo el sistema completo para que vendas tus servicios online sin complicaciones.
            </h2>
        </div>
        <div class="circles">
            <div class="circle large"></div>
            <div class="circle small"></div>
        </div>
    </div>
</div>

<style>
    .hero {
        padding: 20px;
        min-height: 50vh; /* Restamos la altura del menú */
        display: flex;
        flex-direction: column;
        justify-content: center;
        position: relative;
        overflow: hidden;
        margin-top: 60px; /* Añadimos un margen superior igual a la altura del menú */
    }

    h1 {
        font-size: 4.2rem; /* Ajustado para el nuevo texto */
        margin-bottom: 2rem; /* Espacio entre h1 y h2 */
        line-height: 1.2;
        position: relative;
        z-index: 2;
        color: var(--color-primario);
        max-width: 800px; /* Limita el ancho para mejor legibilidad */
    }

    .subtitle {
        font-size: 2rem; /* Ajustado para el nuevo texto */
        line-height: 1.5;
        position: relative;
        z-index: 2;
        color: rgb(58, 54, 54);
        max-width: 700px; /* Limita el ancho para mejor legibilidad */
    }

    .circles {
        position: absolute;
        bottom: -50px;
        right: -50px;
        z-index: 1;
    }

    .circle {
        background-color: #11b28f3f;
        border-radius: 50%;
        position: absolute;
    }

    .circle.large {
        width: 200px;
        height: 200px;
        bottom: 0;
        right: 0;
    }

    .circle.small {
        width: 100px;
        height: 100px;
        bottom: 150px;
        right: 150px;
    }

    body::before {
        content: "";
        position: fixed; /* Cambiado de absolute a fixed */
        top: 60px; /* Comienza después del menú */
        left: 0;
        right: 0;
        bottom: 0;
        background-image: radial-gradient(#000 1px, transparent 1px);
        background-size: 20px 20px;
        opacity: 0.1;
        z-index: -1;
    }

    /* Animación del cursor */
    .blinking-cursor {
        font-weight: 400; /* Peso normal para el cursor */
        animation: blink 1s step-end infinite;
    }

    @keyframes blink {
        50% {
            opacity: 0;
        }
    }

    @media (min-width: 768px) {
        .hero {
            min-height: calc(70vh - 60px);
        }
        .titulares {
            margin-bottom: 0; /* Eliminado el margen inferior grande */
        }
        h1 {
            font-size: 5.8rem; /* Ajustado para el nuevo texto */
        }
        .subtitle {
            font-size: 2.2rem; /* Ajustado para el nuevo texto */
        }
        .circle.large {
            width: 400px;
            height: 400px;
        }
        .circle.small {
            width: 400px;
            height: 400px;
        }
    }
</style>
