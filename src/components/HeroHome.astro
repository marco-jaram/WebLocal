<div class="hero">
    <div class="contenedor">
        <div class="titulares">
            <h1>
                Transforma tu conocimiento en un negocio digital automatizado<span class="blinking-cursor">|</span>
            </h1>
            <h2 class="subtitle">
                Dedícate a tus clientes, no a la tecnología. Construyo el sistema completo para que vendas tus servicios online sin complicaciones.
            </h2>
        </div>
        <div class="circles">
            <div class="circle large"></div>
            <div class="circle small"></div>
        </div>
    </div>
</div>

<style>
    /* Base (Mobile) Styles */
    .hero {
        padding: 20px;
        min-height: 50%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        position: relative;
        overflow: hidden;
        margin-top: 60px;
    }

    h1 {
        font-size: 4.2rem;
        margin-bottom: 2rem;
        line-height: 1.2;
        position: relative;
        z-index: 2;
        color: var(--color-primario);
        max-width: 800px;
    }

    .subtitle {
        font-size: 2rem;
        line-height: 1.5;
        position: relative;
        z-index: 2;
        color: rgb(58, 54, 54);
        max-width: 700px;
    }

    .circles {
        position: absolute;
        bottom: -50px;
        right: -50px;
        z-index: 1;
    }

    .circle {
        background-color: #11b28f3f;
        border-radius: 50%;
        position: absolute;
    }

    .circle.large {
        width: 200px;
        height: 200px;
        bottom: 0;
        right: 0;
    }

    .circle.small {
        width: 100px;
        height: 100px;
        bottom: 150px;
        right: 150px;
    }

    body::before {
        content: "";
        position: fixed;
        top: 60px;
        left: 0;
        right: 0;
        bottom: 0;
        background-image: radial-gradient(#000 1px, transparent 1px);
        background-size: 20px 20px;
        opacity: 0.1;
        z-index: -1;
    }

    .blinking-cursor {
        font-weight: 400;
        animation: blink 1s step-end infinite;
    }

    @keyframes blink {
        50% {
            opacity: 0;
        }
    }

    /* Tablet & Desktop Overrides */
    @media (min-width: 768px) {
        .hero {
           margin-top: 0px;
        }
        h1 {
            font-size: 5.8rem;
        }
        .subtitle {
            font-size: 2.2rem;
        }
        .circle.large {
            width: 400px;
            height: 400px;
        }
        .circle.small {
            width: 400px;
            height: 400px;
        }
    }
</style>